[package]
name = "integration_tests"
version = "0.1.0"
authors = ["Exonum Team <exonum@bitfury.com>"]
publish = false
workspace = ".."

[lib]
crate-type = ["dylib"]

[features]

[dependencies]
java_bindings = { path = "..", features = ["invocation"] }
lazy_static = "1.0.0"
