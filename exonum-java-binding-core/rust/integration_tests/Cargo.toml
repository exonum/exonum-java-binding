[package]
name = "integration_tests"
version = "0.1.0"
authors = ["Exonum Team <exonum@bitfury.com>"]
publish = false
workspace = ".."

[features]

[dependencies]
exonum = { version = "0.4" }
java_bindings = { path = ".." }
jni = { version = "0.9.0", features = ["invocation"] }
lazy_static = "1.0.0"
